Character cards here are seeded into a fresh install: .json cards, or Tavern .png cards
(the PNG becomes the avatar).

A .json with an `entries` array is read as a standalone lorebook instead, seeded and attached
to every bundled card.
